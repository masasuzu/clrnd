name: ci

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  # 検査の中身は verify.yml にあり、release.yml も同じものを呼ぶ。
  # ここに直接書くと、リリース前検査と通常 CI がいずれずれる。
  verify:
    uses: ./.github/workflows/verify.yml
